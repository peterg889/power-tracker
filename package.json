{
  "name": "power-tracker",
  "version": "1.0.0",
  "description": "Tracks JCP&L / FirstEnergy outage ETRs over time and grades how accurate their restoration estimates are in aggregate.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "collect": "node src/collector.js",
    "demo": "node src/seed-demo.js",
    "test": "node --test",
    "build:lambda": "bash infra/build-lambda.sh"
  },
  "dependencies": {
    "express": "^4.21.2",
    "undici": "^7.3.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
